import React from 'react';
import { cars, routeMapping } from './carConstants';

const carComponent = () => {

  console.log(cars);
  console.log(routeMapping);

  return (
    <div>
      {}
    </div>
  );
};

export default carComponent;